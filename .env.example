# Configuração do Frontend Next.js
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Porta do backend (opcional - será detectada automaticamente se não fornecida)
NEXT_PUBLIC_BACKEND_PORT=3001

# Host do backend (opcional - padrão: localhost)
# Útil para desenvolvimento com Docker ou backend remoto
# NEXT_PUBLIC_BACKEND_HOST=localhost

# Protocolo do backend (opcional - padrão: http)
# Use https para produção ou quando o backend usar SSL/TLS
# NEXT_PUBLIC_BACKEND_PROTOCOL=http

# Range de portas para busca automática (opcional - padrão: 5)
# Define quantas portas sequenciais tentar após a porta inicial
# NEXT_PUBLIC_BACKEND_PORT_RANGE=5

# Timeout para verificação de health check em milissegundos (opcional - padrão: 1000)
# Útil para conexões mais lentas
# NEXT_PUBLIC_BACKEND_TIMEOUT=1000
